{
  "name": "shooter-web",
  "version": "1.0.0",
  "private": true,
  "description": "Browser terrain shooter prototype with physics, bots, grapple, and destructible terrain.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:pages": "rm -rf docs && mkdir -p docs && cp -R public/. docs/ && touch docs/.nojekyll",
    "sync-pages": "npm run build:pages"
  },
  "engines": {
    "node": ">=18"
  }
}
